Tkinter adalah module built-in python dan memerlukan pemahaman OOP dalam penggunaannya
Module ini digunakan untuk GUI (Graphical User Interface)

import tkinter (buat gui)
Module :
    Tk():
        window = Tk()
        window.title("")
            sebagai judul atau heading diatas pop-up windownya 
        window.mainloop()
            ini akan menjaga windownya ga langsung hilang begitu program dijalankan.
            disini kita tidak bisa menggunakan while true: didalam programnya.
    
    Label():
    label = Label(text="", font="")

    Entry():
    
    Button():
